'use client';
/** หน้าหลัก — ต้นหลักของกองการพยาบาล (รวมใบของทุกหน่วยงานไว้ต้นเดียว) */
import TreeDashboard from '@/components/dashboard/TreeDashboard';

export default function HomePage() {
  return <TreeDashboard />;
}
